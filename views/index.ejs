<%- include('./partials/head') %>

<h2>Students</h2>
<ul>
  
  <% persons.forEach(person => { %>
    <!-- <li><a href="./person/<%= person.id %>"><%= person.name %></a>, squad_id <%= person.squad_id %></li>-->

    <li>
      
        <article>
    <!-- href="./person/<%= person.id %>" -->
          <!-- <label for="<%= person.id %>"> -->
          <img src="<% if (person.avatar) { %>
          <%= person.avatar %>
        <% } else { %>
          /image/unknown.webp
          <% } %>" alt="">

          <h2><%= person.name %></h2>
          <a href="https://fdnd.directus.app/items/person/<%= person.id %>">Lees meer</a>
        <!-- <input type="radio" name="npc" id="<%= person.id %>"> -->
          
        <!-- </label>, squad_id <%= person.squad_id %> -->
        <!-- <%= person.name %>hoi -->
      </article>
    <!-- </a> -->
  </li>
  <% }) %>
</ul>

<%- include('./partials/foot') %>